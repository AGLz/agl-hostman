# Archon v0.4 — CT183 (archon @ AGLSRV1)
# Supabase: CT184 ONLY — http://192.168.0.184:8000 (NAO usar Supabase no CT183)
# Copiar para /opt/archon/.env

PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
DEFAULT_AI_ASSISTANT=claude

# Persistência no host (LXC)
ARCHON_DATA=/opt/archon-data
ARCHON_USER_HOME=/opt/archon-user-home

# PostgreSQL local (--profile with-db)
POSTGRES_PASSWORD=changeme-archon-pg-ct183
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:changeme-archon-pg-ct183@postgres:5432/remote_coding_agent

# Claude — preencher antes de workflows em produção
# CLAUDE_CODE_OAUTH_TOKEN=
# CLAUDE_API_KEY=
# CLAUDE_USE_GLOBAL_AUTH=false

# LiteLLM AGL (opcional — configurar também em /.archon/config.yaml se necessário)
# ANTHROPIC_BASE_URL=http://192.168.0.186:4000

MAX_CONCURRENT_CONVERSATIONS=10
