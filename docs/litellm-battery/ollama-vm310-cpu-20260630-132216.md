# Ollama VM310 — benchmark CPU (sem GPU)

**Data:** 2026-06-30  
**Host:** VM310 `agl-ollama` (`100.67.253.52:11434`)  
**Contexto:** GPUs removidas; Ollama reconfigurado para CPU-only (`zz-cpu.conf`).

## Ajustes aplicados

1. **Ollama drop-in** `ollama.service.d/zz-cpu.conf` + `ollama-gpu1.service.d/zz-cpu.conf`:
   - `OLLAMA_NUM_GPU=0`, `OLLAMA_VULKAN=0`, flash attention off
2. **Proxmox VM310:** `balloon=0`, `numa0 memory=24576 policy=preferred` (antes 16384 bind)

## Resultados (warm, num_ctx=1024, num_predict=32, think=false para qwen)

| Modelo | load_ms | wall_ms | eval | tok/s |
|--------|---------|---------|------|-------|
| **llama3.1:8b** | 210.7 | 40981 | 32 | **0.85** |
| gemma4-qat | 901.2 | 39164 | 11 | 0.36 |
| qwen3:8b | 401.1 | 46072 | 11 | 0.29 |
| qwen3:4b | 312.5 | 122139 | 32 | 0.28 |

## Decisão

**Modelo primário CPU VM310:** `llama3.1:8b` (~3× mais rápido que qwen3/gemma neste hardware).

> Nota: tok/s global ainda baixo (~0.85) vs GPU histórica; workload CPU-only limitado pelo hardware actual da VM310 pós-remoção GPU.

