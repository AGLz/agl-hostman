# Auditoria CT186 — LiteLLM providers (2026-07-09)

## Resumo

| Área | Estado |
|------|--------|
| LiteLLM proxy | Healthy; aliases cloud OK após fix YAML |
| Ollama VM310 | Saturável com prompts longos Hermes |
| OpenAI / Anthropic | Quota / token inválido |
| Z.AI glm-4.5-flash | OK via proxy |
| Groq | OK — fallback principal |
| OpenRouter direct | IDs free inválidos (aliases zero-OR → Groq/Z.AI) |

## Correções neste commit

1. **YAML** — `patch_config_vm310_ollama.py`: indentação `- litellm_params:` + `yaml.safe_load` pré-deploy.
2. **Rate limits Ollama** — `agl-primary` rpm=6/tpm=48k; fast rpm=8; strong rpm=4.
3. **Concorrência** — `max_parallel_requests: 32`.
4. **GPU single** — `VM310_SINGLE_GPU=1`: strong e aliases em `:11434`.
5. **Primário GPU** — `qwen3:4b` ctx 32k (~41 tok/s benchmark 2026-07-08).

## Política de tiers

- Cloud paid → Z.AI flash / Groq quando quota OK
- `agl-primary-fast` — respostas curtas (gemma4-qat)
- `agl-primary` — default local (qwen3:4b)
- `agl-primary-strong` — raciocínio (qwen3:8b)

Hermes: compressão contexto via `fix-hermes-rate-limits-ct188.sh`.

## Comandos

```bash
bash scripts/litellm/apply-litellm-vm310-ollama.sh
bash scripts/litellm/audit-providers-hermes.sh
```
