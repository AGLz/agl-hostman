# Auditoria CT186 — LiteLLM providers (2026-07-09)

## Resumo executivo

| Área | Estado |
|------|--------|
| **LiteLLM proxy** | Healthy após fix YAML; aliases cloud OK |
| **Ollama VM310** | Saturado (prompts longos Hermes); `agl-primary` timeout |
| **OpenAI** | Quota esgotada (todos os modelos) |
| **Anthropic** | Token inválido |
| **Z.AI glm-4.5-flash** | OK via proxy; rate limit em probes directos |
| **Z.AI glm-5** | Quota semanal (reset ~18:32 UTC-3) |
| **Groq** | OK — fallback principal quando cloud falha |
| **Gemini** | AUTH (key + credenciais misturadas) |
| **DeepSeek** | Saldo insuficiente (402) |
| **OpenRouter direct** | IDs free inválidos (aliases zero-OR redireccionam para Groq/Z.AI) |

## Correções aplicadas nesta sessão

1. **YAML crash loop** — `patch_config_vm310_ollama.py` gerava indentação inválida (`  - litellm_params:`); corrigido para `- litellm_params:` + validação `yaml.safe_load` antes de deploy.
2. **Rate limits Ollama** — `agl-primary` rpm=6/tpm=48k, `agl-primary-fast` rpm=8/tpm=32k, `agl-primary-strong` rpm=4/tpm=32k, `agl-primary-vm110` rpm=4/tpm=24k.
3. **Concorrência global** — `max_parallel_requests: 32` em `litellm_settings`.
4. **Timeout primário** — `agl-primary` timeout 120s (antes 12s em patch anterior).
5. **Hermes CT188** — `fix-hermes-rate-limits-ct188.sh` (compressão contexto + fallbacks resilientes).

## Auditoria proxy (r2 — container healthy)

Relatório completo: `audit-ct186-20260709-r2.txt`

### Aliases OK

| Alias | Upstream típico | Latência |
|-------|-----------------|----------|
| gpt-5-mini/nano/4o-mini | glm-4.5-flash (Z.AI) | 8–17s |
| glm-4.7-flash | groq llama-3.1-8b | ~5s |
| zai-glm-flash / zai-glm-5 | direct Z.AI | ~1s |
| gemini-lite | gemini-lite | ~11s |
| or-*-free | groq/zai (zero-openrouter) | <250ms |

### Aliases FAIL

| Alias | Causa |
|-------|-------|
| `qwen-coder` | Modelo não registado no proxy |
| `agl-primary` / `ollama-qwen3-4b` | VM310 saturada (runner >60min CPU, ctx 32k) |
| Ollama direct qwen3:8b | Timeout 60s — fila VM310 |

## Política de tiers (best practices)

```
Hermes / agentes
    │
    ▼
LiteLLM CT186 (max_parallel=32)
    │
    ├─► Cloud paid (OpenAI/Anthropic) — QUOTA/AUTH → skip
    ├─► Z.AI flash / Groq — tier rápido cloud (rpm limitado)
    ├─► agl-primary-fast (gemma4-qat) — respostas curtas, 43 tok/s
    ├─► agl-primary (qwen3:4b) — default local, rpm=6, ctx≤32k
    └─► agl-primary-strong (qwen3:8b) — raciocínio, rpm=4
```

### Regras operacionais

1. **Não enviar prompts >8k tokens para Ollama** — Hermes deve usar `compression.enabled` (threshold 0.65).
2. **Benchmarks VM310** — pausar `litellm-proxy` ou usar script sequencial com unload.
3. **OpenRouter free** — não chamar IDs directos; usar aliases `or-*-free` no proxy.
4. **Renovar keys** — OpenAI quota, Anthropic token, Gemini AUTH, DeepSeek saldo.
5. **Z.AI glm-5** — reservar para coding (`zai-coding-glm-4.7` rpm=2); quota semanal monitorizar.

## Comandos úteis

```bash
# Auditoria
bash scripts/litellm/audit-providers-hermes.sh

# Deploy GPU Ollama
bash scripts/litellm/apply-litellm-vm310-ollama.sh

# Hermes compressão
ssh root@100.81.225.22 'bash /mnt/overpower/apps/dev/agl/agl-hostman/scripts/proxmox/fix-hermes-rate-limits-ct188.sh'

# Estado VM310
ssh root@100.67.253.52 'curl -s http://127.0.0.1:11434/api/ps | jq'
```

## Próximos passos

- [ ] Smoke `agl-primary` quando VM310 libertar runner (ou restart `ollama-gpu0` em janela)
- [ ] Registar alias `qwen-coder` ou remover do audit Hermes
- [ ] Corrigir `GEMINI_API_KEY` / credenciais misturadas no `.env` CT186
- [ ] Monitorizar Z.AI quota reset 2026-07-09 18:32
