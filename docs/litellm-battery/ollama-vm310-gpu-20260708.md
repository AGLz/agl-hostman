# Ollama VM310 — benchmark GPU (RX 580, 1× GPU)

**Data:** 2026-07-08  
**Host:** VM310 `agl-ollama` (`100.67.253.52:11434`)  
**GPU:** AMD RX 580 2048SP — Vulkan, `size_vram` confirmado em todos os modelos.

## Pré-requisitos aplicados

- Passthrough `hostpci0: 0000:02:00.0` no Proxmox
- `linux-modules-extra` + `amdgpu` + `mesa-vulkan-drivers`
- Removido `zz-cpu.conf`; Ollama GPU (`OLLAMA_VULKAN=1`)
- LiteLLM pausado durante benchmark (CT186 saturava fila com prompts ~50k tok)

## Resultados (warm, num_ctx=2048, num_predict=32)

| Modelo | tok/s | wall_ms | load_ms | proc | VRAM GB |
|--------|-------|---------|---------|------|---------|
| **gemma4-qat** | **42.73** | 8130 | 7115 | GPU | 4.44 |
| qwen3:4b | 40.81 | 5834 | 4745 | GPU | 3.50 |
| llama3.1:8b | 28.90 | 6946 | 5417 | GPU | 5.46 |
| qwen3:8b | 26.62 | 8460 | 7543 | GPU | 5.94 |

## Decisão LiteLLM

| Alias | Modelo | Motivo |
|-------|--------|--------|
| `agl-primary` | `qwen3:4b` | ctx longo (32k), ~41 tok/s, menor VRAM |
| `agl-primary-fast` | `gemma4-qat` | máximo tok/s (~43), ctx 8k |
| `agl-primary-strong` | `qwen3:8b` | modelo maior, GPU1 ou :11434 |

## Branch / dev

- Workspace em **`develop`** (sync com `origin/develop`, inclui makemoney PR #29)
- Scripts VM310 CPU/GPU em `develop` (commit `1579a3b2dd`)
- CT186 tinha deploy CPU legado (`llama3.1:8b`) — reposto para GPU via `apply-litellm-vm310-ollama.sh`

## Nota operacional

CT186 com `agl-primary` → Ollama gera contenção na fila (`OLLAMA_NUM_PARALLEL=2`). Monitorizar prompts grandes via LiteLLM; considerar `rpm`/`max_parallel_requests` por alias Ollama.
