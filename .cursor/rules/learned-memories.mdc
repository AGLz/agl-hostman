---
trigger: model_decision
description: Project-specific knowledge, conventions, and user preferences learned by the AI assistant.
---

# Project Memory

Este ficheiro guarda conhecimento específico do projeto, convenções e preferências do utilizador aprendidas durante as interações. Consultar antes de propor soluções ou alterações de config.

## User Preferences

- **Idioma:** responder sempre em Português.

## Technical Decisions

### OpenRouter — política de créditos (2026-06-29)

- **Não gastar os créditos da OpenRouter** em condições normais. O depósito de **≥$10** desbloqueia o teto de **1.000 req/dia** nos free models (vs. 50/dia); manter o saldo intacto vale mais do que consumir tokens pagos.
- **Saldo > $30:** pode usar modelos **paid** (`or-*` sem `-free`).
- **Saldo $15–$30:** preferir free; paid só em casos pontuais justificados.
- **Saldo < $15:** **limitar a free** (`or-*-free`, `openrouter-free`) + `groq-llama-31-8b` + `agl-primary-vm110`; não acionar paid em fallback automático.
- **Saldo < $10:** repor créditos com urgência (perde o teto de 1.000/dia).
- Verificar saldo: `GET https://openrouter.ai/api/v1/credits`.
- Detalhe completo na wiki: `llm-wiki/wiki/OpenRouter.md`.

### OpenRouter — estado dos modelos (validação 2026-06-29)

- Maioria dos `:free` antigos **deprecada/indisponível** (404).
- **`or-owl-alpha`**: melhor free all-round para Hermes (1M ctx, ~19 tps, stealth — prompts logados).
- **`or-nemotron-ultra-free`**: melhor free para reasoning/orquestração (550B/55B MoE, 1M ctx, ~7 tps — prompts logados NVIDIA).
- **`or-nemotron-super-free`**: fallback rápido (262K ctx, ~1.2s latência).
- Paid funcionais (só com saldo > $30): nemotron-super, minimax-m2.5, llama-3.3-70b, gpt-4o-mini, deepseek-v3, gemma-3-27b.
- **`max_tokens` baixo → `content` vazio** em modelos com reasoning (nemotron/minimax), igual ao GLM; usar `max_tokens` ≥ 4096–8192.

### Hermes — primários OpenRouter free (2026-06-29)

- Script: `scripts/proxmox/hermes-openrouter-free-ct188.sh`
- jarvis/curator → `or-nemotron-ultra-free`; elon/satya/werner/orion → `or-owl-alpha`
- Fallbacks: groq → `agl-primary-vm110` (dados sensíveis → local)

### Hermes — modelo Verdent Manager (2026-06-29)

- **Jarvis = Manager, não executor.** Loop **Plan→Execute→Verify→Deliver**: clarifica → decompõe (fases→subtasks→**acceptance criteria**) → delega (`delegate_task`, paraleliza ortogonais) → `read_agent_context` antes de re-delegar → gate **Verifier** → entrega só o que precisa de decisão.
- **Verifier** = 8º agente, gate QA **PASS/FAIL** (modelo `@Verifier`); container `agl-hermes-verifier`. Não implementa correções nem decide prioridades.
- **Review-queue:** `/opt/llm-wiki/raw/hermes/review-queue/queue.json` (rw via `LLM_WIKI_DIR`; **`/mnt/overpower` é NFS root-squashed**, não escrevível pelos agentes). Helper: `scripts/proxmox/hermes-review-queue.sh`.
- **Stand-up cron 2h** (`jarvis-standup-2h`): Jarvis varre `read_agent_context` + review-queue.
- **Crons:** Jarvis só gerencial (briefing, emails, stand-up). Executor migrado → Werner (infra) / Elon (research) / Satya (makemoney). Script: `migrate-hermes-jarvis-crons-ct188.sh`.
- **Roster (8) da AGLz AI Agency:** Jarvis, Elon, Satya, Werner, Orion, Argus, Curator, **Verifier**.
- **Não criar páginas/agências novas:** a agência é a **AGLz AI Agency** (runtime Hermes). Doc canónica: `llm-wiki/wiki/Hermes — Operações CT188.md` + `agl-hostman/docs/HERMES-AGENCY-AGENTS.md`. Modelo: `llm-wiki/wiki/Verdent Manager.md`.

## Project Conventions

- LiteLLM gateway em **CT186** (`100.125.249.8:4000`); deploy via `scripts/litellm/deploy-litellm-callbacks-ct186.sh`.
- Validar YAML do `config/litellm/config.yaml` dentro do container `litellm-proxy` (host não tem `pyyaml`).
- Conhecimento durável → **llm-wiki** (`/mnt/overpower/apps/dev/agl/llm-wiki/wiki/`); atualizar `index.md`/`log.md` após mudanças relevantes.
