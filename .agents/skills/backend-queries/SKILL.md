---
name: Backend Queries
description: Write efficient, secure database queries with proper parameterization, eager loading, optimization techniques, and SQL injection prevention. Use this skill when writing database queries using SQL or ORM query builders in files like queries/, repositories/, services/, or query-related modules, implementing data fetching and filtering logic, optimizing queries for performance through indexing and query planning, setting up eager loading or joins to prevent N+1 query problems, using SELECT statements with specific columns (avoiding SELECT *) for better performance, implementing query transactions for maintaining data consistency across related operations, adding query timeouts to prevent runaway queries from impacting system performance, setting up query result caching for complex or frequently-run queries, working with query builders, repository patterns, or service layers, writing parameterized queries and using ORM methods to prevent SQL injection (never interpolating user input into SQL strings), indexing columns used in WHERE, JOIN, and ORDER BY clauses, wrapping related database operations in transactions, implementing pagination and limiting result sets, using database views or materialized views for complex queries, or optimizing JOIN operations and subqueries. Essential for maintaining database security, achieving optimal query performance, preventing common database vulnerabilities, and ensuring data consistency in multi-step operations.
---

# Backend Queries

This Skill provides Codex with specific guidance on how to adhere to coding standards as they relate to how it should handle backend queries.

## When to use this skill:

- Writing database queries using SQL or ORM query builders
- Implementing data fetching and filtering logic
- Optimizing queries for performance
- Setting up eager loading or joins to prevent N+1 queries
- Using SELECT statements with specific columns (avoiding SELECT *)
- Implementing query transactions for data consistency
- Adding query timeouts and error handling
- Setting up query result caching
- Working with query builders, repositories, or service layers
- Writing parameterized queries to prevent SQL injection
- Working with files in `queries/`, `repositories/`, `services/`, or query-related modules

## Instructions

For details, refer to the information provided in this file:
[backend queries](../../../agent-os/standards/backend/queries.md)
