# Defaults EvoNexus / LiteLLM — sem DashScope (2026-05)
# Copiar linhas relevantes para /workspace/config/.env no CT242 e recarregar providers.

# Terminal Claude (API Anthropic → LiteLLM)
ANTHROPIC_MODEL=glm-4.7-flash
ANTHROPIC_DEFAULT_HAIKU_MODEL=glm-4.7-flash
ANTHROPIC_DEFAULT_SONNET_MODEL=glm-5
ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-7
EVONEXUS_ANTHROPIC_MODEL=glm-4.7-flash

# Provider litellm/openclaude no scheduler (OpenAI-compat) — NÃO usar qwen-coder
OPENAI_MODEL=glm-4.7-flash

# Desactivar uso acidental (opcional: remover key do .env)
# DASHSCOPE_API_KEY=
