# LiteLLM CT186 — fallback API path (virtual key em env local, nunca commitar)
export LITELLM_GATEWAY_URL="${LITELLM_GATEWAY_URL:-http://100.125.249.8:4000}"
export ANTHROPIC_BASE_URL="${LITELLM_GATEWAY_URL}"
# Definir localmente (600): export LITELLM_VIRTUAL_KEY=sk-...
export ANTHROPIC_AUTH_TOKEN="${LITELLM_VIRTUAL_KEY:-}"
unset ANTHROPIC_API_KEY 2>/dev/null || true
