#!/usr/bin/env bash
# Legado: preferir docker-compose.opt-litellm.yml via replicate-all-hosts.sh
set -euo pipefail
echo "Este patch foi substituído por deploy do compose canónico (docker-compose.opt-litellm.yml)."
exit 0
