# SymmetricDS engines

Colocar aqui ficheiros `.properties` por nó:

- `ct610-sild.properties` — node CT610, base SILD
- `vm620-sild.properties` — node VM620, base SILD

Gerados pelo script `scripts/mssql-sync/generate-symmetricds-pilot.sh` após definir `mssql-sync.env`.

Documentação: `docs/maint/MSSQL-SYNC-AGLSRV6-ARCHITECTURE.md`
