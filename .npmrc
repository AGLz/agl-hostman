# Corrige warnings npm 11 (cache-min deprecated, network-timeout desconhecido)
# Sobrescreve /root/.npmrc para este projeto
prefer-offline=true
fetch-timeout=300000

# Ruflo: bug "Invalid Version" em dependência agentic-flow/@claude-flow
# Workaround: usar ruflo já instalado no agldv03 ou aguardar fix upstream
